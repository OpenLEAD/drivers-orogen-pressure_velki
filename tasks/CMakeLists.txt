# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(pressure_velkiTaskLib)
ADD_LIBRARY(${PRESSURE_VELKI_TASKLIB_NAME} SHARED 
    ${PRESSURE_VELKI_TASKLIB_SOURCES})
add_dependencies(${PRESSURE_VELKI_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${PRESSURE_VELKI_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PRESSURE_VELKI_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PRESSURE_VELKI_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PRESSURE_VELKI_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PRESSURE_VELKI_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PRESSURE_VELKI_TASKLIB_HEADERS}
    DESTINATION include/orocos/pressure_velki)

